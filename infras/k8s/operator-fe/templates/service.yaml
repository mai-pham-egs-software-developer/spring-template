apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-fe.fullname" . }}
  labels:
    {{- include "operator-fe.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "operator-fe.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
